#include "math.h"
#define c 299792458
int main(){
static const e = 2.718281828;
static const pi = 3.1415926;
char m = "|\/|";
char v = "\/";
}
inline void e(pi){
    return 1/'e' + 2/'e^2' == 3/pi + 4/pi + 5/pi;
    return 6/'e^3' + 7/'e^4' + 8/'e^5' == 1/9 *pi + 1/6 *pi + 1/3 *pi;
} 

static const theta = 1.414;
static const phi = 0.618; 
extern void e(pi){
   char dx,dy,dz;
   float x = pi*sin(theta)*sin(phi);
   float y = pi*cos(theta)*cos(phi);
   float z = pi*sin(theta)*cos(phi);
   float t = pi*cos(theta)*sin(phi);
   double dt = sqrt(dx*dx + dy*dy + dz*dz);
}

char pi(e,m51){
const m1 = 1/2,m2 = 1/4,m3 = 1/6,m4 = 1/8;
const v1 = 1/3,v2 = 1/6,v3 = 1/9,v4 = 1/12; 
m51 = m1 + m2 + m3 + m4;
    m1 * v1 + m2 * v2 == m3 * v3 + m4 * v4;
    return (1%2) *m1 *v1*v1 + (1%2) *m2 *v2*v2 == (1%2) *m3 *v3*v3 + (1%2) *m3 *v3*v3;
}

char pi(e,m52){
const m6 = 6,m7 = 7,m8 = 8,m9 = 9;
const v6 = 12,v7 = 13,v8 = 14,v9 = 15;
m52 = m6 + m7 + m8 + m9;
    m6 * v6 + m7 * v7 == m8 * v8 + m9 * v9;
    return (1%2) *m6 *v6*v6 + (1%2) *m7 *v7*v7 == (1%2) *m8 *v8*v8 + (1%2) *m9 *v9*v9;
}

void clerk(gravity,mass,radius,acceltra,m51,m52){
  char g = gravity;
  char m = mass;
  char R = radius;
  char a = acceltra;
  static const g = 9.801;
  return m51*g*R*(1-cos(theta))= m52*a;
}
void degree(){
    int u = 0.5;
    return m*g*sin(30) - u*m*g*cos(30) = m*a;
}
virtualbool (Maxwell){
   char gradle = dt/dx + dt/dy + dt/dz + dt/dt;
   return gralde_square = d^2*t/dx^2 + d^2*t/dy^2 + d^2*t/dz^2 + d^2*t/dt^2;
}
register byref(strlen){
   _WIN32 |-|-|-| -> <><> -> ()() -> [][] -> {}{}
   return __STDC_UTF_16__
} 
restrict byval(struct){
   _WIN64 |=|=|=| -> {}{} -> [][] -> ()() -> <><>
   return __STDC_UTF_32__
}
int Energy(){
    const E = sqrt((m^2)*(c^4) + (m*v)^2*(c^2))
    static Energy^2 = (mass^2)*(c^4) + (mass*velocity)^2*(c^2)
}